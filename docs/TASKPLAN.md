# Lien — 実装計画書(タスク順序・並列性・人間ゲート)

- 作成日: 2026-06-10
- 対象読者: プランナー(/plan)・自律ループ・人間(あなた)
- 使い方: `/plan` は本書のフェーズ単位でタスクを BACKLOG 化する(ID をタスク文頭に残す)。**BACKLOG に積むのは「いま実行可能」なタスクだけ** — 人間ゲート待ちのタスクを積むとループが BLOCKED で全停止する。ゲート通過後に次のバッチを積む

## 0. 実行者タグ

| タグ | 意味 |
|---|---|
| [W] | Windows の自律ループで完結する(コード生成+`deno test`/CI 確認まで) |
| [M] | Mac が必要(xcodegen 実行・実機・TestFlight アップロード)。Mac で対話セッションを開くか、Mac 上でループを回す |
| [H] | 人間にしかできない(アカウント作成・支払い・実機の物理操作・審査・好みの判断) |

## 1. 人間ゲート一覧(あなたの TODO。ここが詰まると全体が止まる)

| ゲート | タイミング | 内容 | 所要 |
|---|---|---|---|
| **G0 開発基盤** | **最優先(実装と並行して今すぐ)** | ① Apple Developer Program 加入(¥12,800/年。審査に最大48h) ② Supabase プロジェクト作成 ③ Bundle ID 決定(推奨: `com.ryoki63.lien`) ④ APNs 認証キー(.p8)発行 ⑤ シークレットを所定の場所へ(DESIGN §7 の表どおり) ⑥ GitHub Actions 有効確認 | 1〜2時間+待ち時間 |
| **G1 M0実機検証** | M0 完了時 | iPhone 2台にビルド導入し、片方のチェックイン→もう片方のウィジェット更新を10回計測、p50≦60秒を確認して JOURNAL に記録 | 30分 |
| **G2 ドット絵承認** | M1 中(T09 提出後) | 植物スプライト第1案(13枚)からトーンを選ぶ(好みの判断。差し戻しOK) | 15分 |
| **G3 TestFlight配布** | M2 完了時 | App Store Connect にアプリ作成 → 内部テスト → 外部テスト審査(1〜2日) → 知人5〜10ペアへ招待。**Pairy 終了(2026-06-30)より前に配布できると乗り換え流入を取り込める** | 1時間+待ち |
| **G4 ストア素材承認** | M3 中 | 表示名(例:「Lien − ふたりの約束」)・スクショ・説明文・プライバシーポリシー/利用規約の最終確認。独自ドメイン取得の判断(任意、ユニバーサルリンク用) | 30分 |
| **G5 審査提出** | M3 末 | 提出ボタン+リジェクト時の往復対応 | 都度 |
| **G6 課金開始(v1.1)** | v1.1 着手時 | RevenueCat アカウント / App内課金商品の作成(月700円・年2,400円) / トライアル日数の最終決定 / 特商法表記の確認 | 1時間 |

## 2. フェーズ(日付は切らない。出口条件を満たしたら次へ)

実装はエージェントの速度で進むため、律速は人間ゲートの待ち時間(Apple審査48h・TestFlight外部審査1〜2日)だけ。唯一意識する外部日付は **Pairy サービス終了 = 2026-06-30**。TestFlight 配布(G3)がこれより前だと乗り換え流入ボーナスがある。

| フェーズ | 出口条件 |
|---|---|
| M0 技術検証 | G1 合格(プッシュ→ウィジェット p50≦60秒) |
| M1 コアループ | 実機2台で 招待→ペア成立→約束→チェックイン→ストリーク→つつく→通知 が一気通貫 |
| M2 仕上げ+TestFlight | G3 完了、5〜10ペアが使い始めている |
| M3 改善+審査 | App Store リリース |
| M4 v1.1 課金 | ペイウォール稼働、最初の課金者が出る |

## 3. タスク表

依存のないタスク同士は順不同(=並列可)。`→` は依存。

### M0: 技術検証(最難関を最初に倒す)

| ID | タスク | 依存 | タグ | 完了条件 |
|---|---|---|---|---|
| T01 | `products/lien/` 雛形(全ディレクトリ+README+.gitignore+CI骨格) | — | W | 構成が DESIGN §2 と一致。deno test job が CI で緑 |
| T02 | migration 0001: 全テーブル+RLS+`is_pair_member`(REQUIREMENTS §5) | T01 | W | SQL レビュー(サブエージェント検証)で RLS 方針(DESIGN §5.2)との一致を確認 |
| T03 | `_shared/streak.ts`+`tickets.ts`(純粋ロジック)+全エッジケースの deno test | T01 | W | `deno test` 緑(DESIGN §5.4 の例3件+エッジ3件を含む) |
| T04 | functions: `checkin`+`snapshot`+`_shared/apns.ts`(モック注入可)+`snapshot.ts` | T02, T03 | W | `deno test` 緑(APNs はモック) |
| T05 | functions: `invite-create`/`invite-accept`/`close-day`/`grant-tickets` | T04 | W | `deno test` 緑 |
| T06 | ios: `project.yml`+3ターゲット骨格+iOSビルド CI | T01 | W | CI の iOS ビルド緑 |
| T07 | ios: AppGroupStore+PairSnapshot+静的ウィジェット(S)+NSE 骨格 | T06 | W | CI 緑+LienTests 緑 |
| T08 | 【G0後】Supabase へ migrate+functions デプロイ、実機2台で G1 計測 | G0, T04〜T07 | M+H | p50≦60秒 を JOURNAL に記録 |

並列レーン: バックエンド {T02→T03→T04→T05} ∥ iOS {T06→T07} ∥ T09(アセット、M1から前倒し可)

### M1: コアループ

| ID | タスク | 依存 | タグ | 完了条件 |
|---|---|---|---|---|
| T09 | 植物スプライト v0(13枚: 土1+5段階×通常/喜び+しょんぼり2)+取込スクリプト | T01 | W | assets/plant/ に原寸PNG13枚+Asset Catalog 生成が CI 緑 → **G2 へ提出** |
| T10 | 匿名認証+オンボ3画面(コンセプト→ニックネーム→通知許可) | T08 | W | CI 緑+主要パスの ViewModel テスト緑 |
| T11 | 招待 UI(リンク/QR/コード入力)+`web/invite/` 静的ページ | T10 | W | CI 緑。ページは手元ブラウザで動作確認 |
| T12 | 約束設定 UI+API 接続 | T10 | W | CI 緑 |
| T13 | ホーム画面(植物+2人の今日+ストリーク+チェックインボタン) | T10, T09 | W | CI 緑 |
| T14 | チェックインフロー(楽観更新+オフラインキュー+写真添付+当日取消) | T13 | W | CI 緑+キューの単体テスト緑 |
| T15 | つつく+リアクション(UI+functions `nudge`/`react`) | T14 | W | deno test+CI 緑 |
| T16 | 通知一式(リマインドのローカル通知+messages.ts 全文言+設定トグル) | T14 | W | deno test+CI 緑。文言は罰なし原則チェック付き |
| T17 | ウィジェット実データ化(S/M/ロック2種)+NSE 本実装 | T14 | W | CI 緑+スナップショット解釈テスト緑 |
| T18 | カレンダー履歴画面(両者達成/お休み/途切れの表示) | T14 | W | CI 緑 |
| T19 | チケット残数表示+お休み宣言 UI | T14 | W | CI 緑 |
| T20 | M1 一気通貫の実機 E2E(チェックリスト: 招待〜つつくまで12項目) | T11〜T19 | M+H | 全項目パスを JOURNAL に記録 |

並列: T11/T12 ∥、T13→T14 のあと {T15, T16, T17, T18, T19} は全て互いに独立(ただし Strings.swift を触るものは直列推奨)

### M2: 仕上げ → TestFlight

| ID | タスク | 依存 | タグ | 完了条件 |
|---|---|---|---|---|
| T21 | アルバム画面(30日制限の表示含む) | T20 | W | CI 緑 |
| T22 | シェア画像生成(縦・正方形、節目トリガー) | T20 | W | CI 緑+生成画像のスナップショットテスト |
| T23 | 設定画面一式+アカウント削除+通報導線 | T20 | W | CI 緑 |
| T24 | Sign in with Apple 連携(機種変更引き継ぎ) | T20 | W | CI 緑 |
| T25 | privacy-policy.md+terms.md(marketing/、Web公開用HTML込み) | — | W | ひな形完成 → G4 で人間レビュー |
| T26 | アプリアイコン+スクショ素材(6.7"/6.1")+ASO文言案 | T09 | W | marketing/ に出力 → G4 |
| T27 | オンボ磨き+空状態+エラー文言の総点検(罰なし原則) | T21〜T24 | W | Strings.swift 全文言のレビューログ |
| T28 | TestFlight ビルド作成・アップロード・外部テスト申請 | T27, G0 | M+H | **G3**: 外部テスター招待が送られた状態 |

### M3: 改善 → 審査(タスクは TestFlight のフィードバックで都度 /plan)

T29 フィードバック反映(都度) / T30 審査チェックリスト消化(REQUIREMENTS §8) [W] / T31 提出+審査対応 [H=G5] / T32 ASO 初期設定(キーワード・Pairy 関連語) [W→G4]

### M4: v1.1(課金)

T33 RevenueCat 組込+ペイウォール(3文脈) [W→G6] / T34 曜日指定 [W] / T35 植物品種追加+鉢替え [W] / T36 「この日のふたり」 [W] / T37 課金商品設定+価格・トライアル最終化 [H=G6]

## 4. 並列実行ガイド

- ループ自体は 1回1タスクの直列。並列にする方法は2つ:
  1. **マシン分担**: Windows ループが [W] を消化している間、Mac の対話セッションで [M] を進める(同じ main ブランチに pull/push。タッチするディレクトリが違うので衝突しない)
  2. **セッション内fan-out**: 互いに独立なタスク群({T15〜T19} など)は、対話セッションから Agent ツール(worktree 分離)で同時進行できる
- 衝突回避の原則: **同時に触ってよいのは別ディレクトリのタスクのみ**(ios/ ∥ supabase/ ∥ assets/ ∥ marketing/ は常に安全)。`Strings.swift`・`project.yml`・migration 連番を触るタスクは直列にする
- ゲート待ちで [W] が枯れたら: 先のフェーズの [W] タスク(T25, T26 など依存の薄いもの)を前倒しして積む

## 5. 月3万円への逆算(KPI ゲート)

| 時点 | 目標 | 未達のときの動き |
|---|---|---|
| G3+1週間 | 10ペア中5ペア以上が D7 で両者継続 | 課金より先にコアループ改善(つつき転換率・通知文言)に集中 |
| リリース+1ヶ月 | 500DL / 招待→ペア成立率40% / D30 両者継続25% | ASO 見直し+開発物語の発信強化(CONCEPT §7) |
| 課金開始+1ヶ月 | ペイウォール転換 4〜6% → 課金者20人(月1.4万円) | 価格・トライアル・ペイウォール文脈の A/B |
| 最終目標 | **課金者43人 = 月3万円達成** | Pairy 乗り換え組と新年需要(1月が習慣アプリ最大の月)を取りにいく |

- 毎週金曜に `/status` で D7継続・ペア成立率・(v1.1後)転換率を JOURNAL に記録する(計測定義は REQUIREMENTS §7)
- 算数の前提: 月3万 = 課金者43人(月700円換算)。新年需要(1月が習慣アプリ最大の月)が最大の追い風になるため、**その前に課金導線を安定させておくのが最重要**

## 6. ブロック時の運用

- [H] ゲート未達で進めないタスクは BACKLOG に積まない(本書に留めておく)
- ループが BLOCKED を出したら: `progress/JOURNAL.md` の質問に答え、該当タスク行を `- [ ]` に戻して再開
- 仕様の矛盾を見つけたタスクは、勝手に解釈せず BLOCKED + JOURNAL に「REQUIREMENTS/DESIGN のどちらを直すべきか」を書く
## 7. 2026-06-11 改定: G0待ち期間のモックファースト方針

- G0(#38)が最大律速のまま停滞し、Pairy 終了(06-30)前の G3 到達が危ういため、§3 の「T08 → T10〜」の順序を改定する。UI/機能タスク(T10〜T19, T21〜T24)は**プロトコル抽象+モック注入**で T08 前に前倒しし、各 issue の完了条件(deno test+iOS シミュレータ CI 緑)を先に消化する
- 差し替えポイント: 実 Supabase 接続は `AuthServicing` / `InviteClient` / `CheckinAPI` / `HistoryRepository` / `RestDeclaring` 等のプロトコル境界に隔離。T08(G0 後デプロイ)完了時に Live 実装を結線し、T20 実機 E2E で一括検証する(「CI 緑だが未結線」状態は各 issue に残作業を明記して許容)
- 競合回避: `Strings.swift` / `project.yml` / `LienApp.swift` を触るタスクは ios-serial レーン1本で直列(順序は BACKLOG どおり)。migration 連番は T14=0002 → T16=0003 → 以降は着手時採番。`messages.ts` の nudge 系文言は T15、streakRisk/milestone は T16 がオーナー。`PlantSprite.swift`+PlantAssets 取込は T13 がオーナー(T17/T22 は再利用)。`_shared/premium.ts` は最初に着手したタスクが作成し他は再利用
- T08/T20/T28/T30/T32 は「準備スライス」(デプロイ CI・G1 ハーネス・E2E ランブック・リリース CI・レビューノート・ストアメタデータ)を Wave 4 で先行コミットし、ゲート開放後の人間作業を最小化する(issue は開いたまま)
- リスク: モックで固定した契約(エラーコード・署名 URL 挙動・APNs)が実環境とズレる可能性 → T08 後のバックエンドスモーク+T20 実機 E2E を必須ゲートとして残す
- M4(T33〜T36)および T34/T35 の前倒しは、Wave 4 の [W] が**実際に枯渇した場合のみ**検討する(条件: T20 完了まで v1.0 経路の意味変更を凍結・migration/snapshot 契約の直列順守)
