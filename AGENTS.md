# AGENTS.md — エージェント憲法

このリポジトリで動くすべてのエージェント(対話セッション・ヘッドレスループ・サブエージェント)が従う最上位の指示書。
他の指示と矛盾したら、このファイルが優先。

## ミッション

**月3万円の継続収益を生むアプリを作る**(2026-06開始)。

- 第1弾プロダクト: **Lien(リアン)** — 2人で約束を1つだけ決めて続ける習慣アプリ(旧称: ふたり習慣)
- ドキュメントの役割: 企画・エビデンス = `docs/CONCEPT.md` / **確定仕様 = `docs/REQUIREMENTS.md`** / 技術設計 = `docs/DESIGN.md` / 実装順序・並列性・人間ゲート = `docs/TASKPLAN.md`。矛盾したら REQUIREMENTS → DESIGN → CONCEPT の順に優先
- 個人課金 月700円(年2,400円)× 課金者43人で月3万円に到達する算数
- 集客チャネルはゼロからスタート → 招待必須設計・シェア画像・通知は相手の名前で、など**集客装置をアプリ自体に内蔵する**

## 原則(技術・判断の基準)

1. **シンプル最優先** — 機能を足す前に削る。「約束1つだけ、静かでシンプル」が差別化そのもの
2. **保守・デプロイが簡単な技術を選ぶ** — 開発はすべてClaude(バイブコーディング)前提。技術スタックは決定済み: **ネイティブ SwiftUI + WidgetKit / Supabase / RevenueCat**(`docs/DESIGN.md` 参照。Expo+EAS案は2026-06-10に撤回。iOSビルドはMacまたはGitHub Actions macOSランナーで行う)
3. **動くものを早く** — 完璧な設計より、検証できる最小の実装。最難関(ウィジェット)から検証する
4. **罰を使わない設計** — プロダクト設計の決定事項は `docs/CONCEPT.md` §5 に従う
5. **迷ったら止まる** — 曖昧な仕様を推測で進めず、BLOCKED にしてユーザーに質問を残す

## リポジトリ構成

| パス | 役割 |
|------|------|
| `AGENTS.md` | このファイル。エージェントの最上位指示 |
| `CLAUDE.md` | Claude Code が毎セッション読み込むエントリポイント(このファイルを参照) |
| `LOOP.md` | 自律ループ1回分の実行手順書 |
| `tasks/BACKLOG.md` | タスクキュー(上から順に処理) |
| `tasks/DONE.md` | 完了タスクのアーカイブ(追記専用) |
| `progress/JOURNAL.md` | 実行履歴・監査ログ(追記専用) |
| `products/` | アプリ本体。1アプリ = 1サブフォルダ(第1弾: `products/lien/`) |
| `docs/` | 企画書・設計メモ・リサーチ結果 |
| `ralph.ps1` | ヘッドレスループランナー |
| `.claude/skills/` | プロジェクトskill(`/plan` `/status` `/sync`) |
| `.claude/settings.json` | 自律実行用の権限許可リスト |

## 動き方

### タスク管理は GitHub Issues が正

- **タスクの正式な一覧・状態は GitHub Issues**(`https://github.com/Ryoki63/Challenge/issues`)。`tasks/BACKLOG.md` はローカル補助キューとして残すが、issueと乖離した場合はissueを優先する
- 新しいタスクを作るときは `gh issue create` でissueを作成してから BACKLOG に書く(分解は `/plan` skill)
- ループ実行時は `LOOP.md` の手順に厳密に従う(1実行 = 1タスク)
- 実行記録は `progress/JOURNAL.md` に必ず残す。JOURNAL と DONE は追記のみ、過去分の編集禁止(→ 下記「JOURNAL こまめ更新ポリシー」)

### GitHub Issues への記録ルール

| タイミング | 操作 |
|---|---|
| タスク着手時 | `gh issue comment <番号> --body "着手: <日時>。<やること一言>"` を投稿 |
| タスク完了時 | `gh issue comment <番号> --body "完了: <日時>。<やったこと要約>"` を投稿してから `gh issue close <番号>` |
| ブロック時 | `gh issue comment <番号> --body "BLOCKED: <理由>。<ユーザーへの質問>"` を投稿し、issueは開いたまま |
| 人間ゲート待ち | issueに `human-gate` ラベルが付いているものはエージェントがcloseしない。人間が対応後にcloseする |

### JOURNAL こまめ更新ポリシー(ユーザーが常時ウォッチ)

`progress/JOURNAL.md` はユーザーがリアルタイムでウォッチしている。**完了時にまとめて書くのではなく、作業の進行に合わせてこまめに追記する**(対話セッション・ループ・サブエージェント問わず全エージェント共通)。

- **着手したらまず書く**: タスク見出し(`## <YYYY-MM-DD HH:mm> — <タスク>`)と「着手」行を最初に追記してから作業に入る
- **節目ごとに追記する**: 重要な判断・つまずき・方針変更・検証の各節目で、その都度1〜数行を追記する(数分〜1ステップごとが目安。完了まで沈黙しない)
- **締める**: 完了/ブロック時に「結果」行を追記して締める
- 新しい記録は常に**末尾へ追記**。直近の詳細エントリは編集・削除しない。ファイルへの書き込み自体が監査ログなので、commit/push を待たず**書けた時点で都度ファイルに反映する**

#### 古い記録のローテーション(肥大化防止)

JOURNAL がだらだら伸びないよう、**古いエントリはマージ・要約・削除して整理する**。

- ファイル先頭(ヘッダ直後)に **`## アーカイブ要約`** セクションを1つ置く。古い詳細エントリはここへ1〜数行に圧縮して移し、元の詳細本文は削除してよい
- 整理の目安: **詳細エントリが約20件 or 1か月分を超えたら**、古い順に要約へ畳む。直近の詳細(目安: 直近20件 / 直近2週間)は詳細のまま残す
- デモ・使い捨ての検証タスクは、要約も最小限または削除でよい(成果物が既に消えているものは特に)
- **要約は「事実を縮める」こと。記録の捏造・改ざんは引き続き禁止**(やったことを無かったことにしない)。重要な判断・ブロック理由・人間ゲートの記録は要約後も残す

### 人間承認プロトコル(承認待ち → モバイル通知 → 再開)

人間(あなた=`@Ryoki63`)の承認・判断が必要になったら、黙って止まらず「**通知して・答えを待って・返信を見て再開する**」閉ループに乗せる。対話・ループ・サブエージェント共通。

**① リクエストする(止まる側)** — 該当 issue にコメントを投稿する:
- 1行目: `🔔 @Ryoki63 [承認待ち] <1行サマリ>`
- 本文: 状況 / 承認・判断してほしいこと / 選択肢(**推奨案を先頭**) / 「このissueに**返信**で決定を教えてください。エージェントが検知して再開します」
- 末尾に**隠しマーカー行** `<!-- lien-agent -->` を必ず付ける(画面では非表示。再開検知に使う)
- ラベル付与: `gh issue edit <番号> --repo Ryoki63/Challenge --add-label "needs-approval,human-gate"`
- `progress/JOURNAL.md` に `【承認待ち】` 節目を追記し、そのタスクは停止(human-gate なので**エージェントは close しない**)

**② 通知する(別アクター)** — `needs-approval` ラベルが付くと **GitHub Actions の bot が `@Ryoki63` をメンションするコメントを自動投稿**し、GitHub Mobile にプッシュを飛ばす(`.github/workflows/approval-ping.yml`)。エージェントは `Ryoki63` 本人として動くため、自分のコメント/自己メンションでは通知が出ない。だから別アクター(bot)からメンションする。

**③ 再開する(動く側 / ループ・セッションの冒頭で必ずチェック)** — `needs-approval` ラベルの issue を走査し、**bot を除いた最新コメント**(=`Ryoki63` の最新コメント)を見る:
- マーカー `<!-- lien-agent -->` を**含まない** → 人間が返信済み = 答えが来た。そのタスクを**最優先で再開**:返信を判断材料に実行 → `needs-approval` ラベルを外す → 続行/完了し JOURNAL に記録
- マーカーを**含む**(エージェントの問いが最新)→ まだ待ち。スキップ
- 人間の返信がさらに質問なら、マーカー付きで回答コメントを投稿し、ラベルは付けたまま再び待つ

> 同一アカウント(`Ryoki63`)で人間とエージェントが発言するため、両者の区別は**隠しマーカーの有無**で行う(エージェントのコメントには常にマーカーを付ける)。bot の ping は author が `github-actions` なので走査から除外する。

## GitHub 同期ポリシー(常時連携)

- **タスク完了 = commit + push がワンセット**。ローカルに溜め込まない
- 作業開始時(ループの最初)に `git pull --rebase` でリモートと同期する
- ブランチは `main` のみ使用(指示がない限りブランチを切らない)
- `git push --force` と公開済み履歴の書き換えは禁止
- リモート `origin` が未設定・到達不能な場合のみ push をスキップし、JOURNAL にその旨を注記する(作業自体は止めない)

## 禁止事項

- APIキー・トークン等のシークレットをファイルにハードコードしない(`.env` を使う。`.env` は `.gitignore` 済み)
- このリポジトリの外(親フォルダ・他プロジェクト)のファイルを変更しない
- GitHub への push 以外の外部送信(API投稿・メール・ストア申請等)は、タスク本文に明記されている場合のみ
- JOURNAL.md / DONE.md の捏造・改ざん(やったことを無かったことにする等)。※古い記録の要約・整理は「JOURNAL こまめ更新ポリシー」のローテーション手順に従う場合のみ可

## 検証ポリシー

- コードを書いたら必ず実行・ビルドして動作確認する(「書けたはず」で完了にしない)
- 可能なら Agent ツール(サブエージェント)に「完了条件を満たすか批判的に確認して」と独立検証を依頼する
- 検証失敗は最大3回まで修正リトライ。それでもダメなら BLOCKED にして止まる
